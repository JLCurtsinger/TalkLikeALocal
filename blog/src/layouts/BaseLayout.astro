---
const title = "Talk Like a Local — Blog";
const description = "Pronunciations, culture, and preservation resources.";
const canonical = new URL(Astro.url.pathname, Astro.site).toString();
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>{title}</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <style>
      :root { color-scheme: light dark; }
      body { font-family: system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, Noto Sans, Helvetica, Arial; margin: 0; }
      .container { max-width: 920px; margin: 0 auto; padding: 2rem; }
      .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 1rem; }
      .card { border: 1px solid rgba(0,0,0,.1); border-radius: 14px; padding: 1rem; }
      .card h2 { margin: 0 0 .5rem; font-size: 1.1rem; }
      .tag { font-size: .85rem; opacity: .7; }
      a { text-decoration: none; }
    </style>
  </head>
  <body>
    <main class="container">
      <slot />
    </main>
  </body>
</html>
